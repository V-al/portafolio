---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString("es-MX", {
			year: "numeric",
			month: "long", // O 'short' si prefieres abreviado (ej. "ago" en lugar de "agosto")
			day: "numeric",
		})
	}
</time>
